/** Visual gap between message bubbles, in pixels. */
export const GAP = 16;

/** Trigger load-more when the scroll offset from the top drops below this many pixels. */
export const LOAD_MORE_THRESHOLD_PX = 50;

/** Treat the viewport as "near bottom" when the distance to the end is less than this many pixels. */
export const NEAR_BOTTOM_THRESHOLD_PX = 100;
